@page "/admin/companies"

<PageTitle>Companies</PageTitle>

<div class="d-flex align-items-center justify-content-between">
    <h1>Companies</h1>
    <NavLink href="/admin/addcompany"><button class="btn btn-dark">Add</button></NavLink>
</div>

@if (companies == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Manager</th>
                <th>Employees</th>
                @* <th></th> *@
            </tr>
        </thead>
        <tbody>
            @foreach (var company in companies)
            {
                <tr>
                    <td>@company.Name</td>
                    <td>@company.Manager?.UserName</td>
                    <td>@company.Employees?.Count()</td>
@*                     <td>
                        <button class="btn btn-danger" @onclick="() => DeleteCompany(company.Id)">Delete</button>
                    </td> *@
                </tr>
            }
        </tbody>
    </table>
}